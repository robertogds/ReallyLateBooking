// Generated by CoffeeScript 1.3.3
(function() {
  var acceptLegalLabel, newAccountLabel;

  Ti.include('/js/newAccountAction.js', '/js/newAccountTable.js');

  root.newAccountView = Titanium.UI.createView({
    backgroundColor: 'transparent',
    width: '100%'
  });

  newAccountLabel = Titanium.UI.createLabel({
    text: L('orRegisterLabel') + ':',
    textAlign: 'center',
    color: '#fff',
    font: {
      fontSize: 14,
      fontWeight: 'bold'
    },
    height: 30,
    top: 55
  });

  acceptLegalLabel = Titanium.UI.createLabel({
    borderWidth: 0,
    top: 330,
    color: "#fff",
    text: Ti.Locale.getString('acceptTerms'),
    left: 8,
    font: {
      fontSize: 10
    }
  });

  root.newAccountView.add(root.facebookRegisterButton);

  root.newAccountView.add(newAccountLabel);

  root.newAccountView.add(acceptLegalLabel);

  root.newAccountView.add(root.newAccountButton);

  root.newAccountView.add(root.newAccountTable);

  root.newAccountWindow.add(root.newAccountView);

  root.showNewAccount = function() {
    Ti.API.info('Entra en shownew account');
    root.tabGroup.activeTab.open(root.newAccountWindow, {
      animated: true
    });
    return Ti.API.info('Sale de shownew account');
  };

}).call(this);
