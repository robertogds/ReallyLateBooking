// Generated by CoffeeScript 1.3.3
(function() {
  var GenericMenuTable;

  GenericMenuTable = (function() {

    function GenericMenuTable(top, rows) {
      var data, table;
      this.view = Titanium.UI.createView({
        background: 'transparent',
        top: top
      });
      table = Titanium.UI.createTableView({
        top: 0,
        height: 44 * rows,
        width: '95%',
        borderWidth: 0,
        borderRadius: 10,
        scrollable: false,
        moving: false
      });
      this.section = Titanium.UI.createTableViewSection();
      data = [];
      data[0] = this.section;
      table.data = data;
      this.view.add(table);
    }

    return GenericMenuTable;

  })();

  root.GenericMenuTable = GenericMenuTable;

}).call(this);
