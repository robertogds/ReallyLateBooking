// Generated by CoffeeScript 1.3.3
(function() {
  var ciudadesButton, logoEdreams, scrollView;

  scrollView = Ti.UI.createScrollView({
    contentHeight: 'auto',
    scrollType: 'vertical',
    showVerticalScrollIndicator: true,
    height: '100%',
    width: '100%'
  });

  root.indexView = Titanium.UI.createView({
    backgroundColor: '#fec52c',
    height: '100%',
    width: '100%',
    top: 0
  });

  root.eDreamsHeader = Titanium.UI.createView({
    backgroundColor: '#00318C',
    height: 90,
    width: '100%',
    top: 0
  });

  logoEdreams = Titanium.UI.createImageView({
    top: 10,
    left: 5,
    image: '/images/logo_EN.png'
  });

  root.titleBar = Titanium.UI.createView({
    backgroundColor: '#e3e4e7',
    height: 40,
    width: '100%',
    top: 90
  });

  root.titleLabel = new root.GenericTitleLabel(10, 15, "Search Hotels").label;

  root.titleLabel.color = 'black';

  ciudadesButton = new root.GenericButton(330, Ti.Locale.getString('change')).button;

  ciudadesButton.addEventListener('click', function(e) {
    return root.loadDeals(root.staticCities[1]);
  });

  root.indexView.add(ciudadesButton);

  root.titleBar.add(root.titleLabel);

  root.eDreamsHeader.add(logoEdreams);

  root.indexView.add(root.eDreamsHeader);

  root.indexView.add(root.titleBar);

  scrollView.add(root.indexView);

  root.indexWindow.add(scrollView);

  Ti.API.info('Pasa por aki');

}).call(this);
